// This module is imported once at startup by `registerButtercutDefaultBlocks`.
// Add your own block / integration registrations here. Any custom block you
// reference by id in `site.config.ts`'s `home.blocks` must be registered
// before the home page renders.
//
// Example:
//
//   import { registerButtercutBlock } from "@/lib/blocks/registry";
//   import { MyHero } from "./blocks/MyHero";
//
//   registerButtercutBlock("hero", MyHero);          // replaces the default hero
//   registerButtercutBlock("my_block", MyBlock);     // new id usable in site.config.ts
//
// Keep this file side-effect only — no default export.

export {};
